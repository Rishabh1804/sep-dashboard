{
  "name": "sep-dashboard-functions",
  "version": "0.0.0-skeleton",
  "private": true,
  "description": "Cloud Functions for SEP Dashboard Phase 2 (Stage B SKELETON — not yet deployed). Separate package: NOT installed by the root web app, NOT exercised by root CI. Track 2 wires deploy + emulator.",
  "type": "module",
  "main": "src/index.js",
  "engines": { "node": "20" },
  "scripts": {
    "lint": "echo 'functions lint — Track 2'",
    "deploy": "echo 'firebase deploy --only functions — Track 2 (needs live project)'"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
